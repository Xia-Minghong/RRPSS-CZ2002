RRPSS Class:

// All data stored in static main methods, no composition relationship exists, clean UML

public static void main{
ArrayList<Reservation> reservations = new ArrayList<Reservation>()
ReservationManager reservationManager = new ReservationManager(...)
reservationManager.loadReservations(reservations)	//Reference

}


ReservationManager Class:
public void loadReservations(ArrayList<Reservation> reservations){
	...call IOManager
	...assign to the arraylist
}

public static void run(ArrayList<Reservation> reservations){
	get input: name
	getReservationByName(name)
}

public getReservationByName(ArrayList<Reservation> reservations, String name){
	...search
	return reservation
}